[project]
name = "ghidra2specs"
version = "0.3.0"
description = "Converter from Ghidra's XML pattern format to fugue-specs YAML format."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "click>=8.3.1",
    "lxml>=6.0.2",
    "pyyaml>=6.0.3",
]
